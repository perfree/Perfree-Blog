project:
  name: PerfreeBlog
  version: 1.0.0
author:
  name: Perfree
  link: https://www.yinpengfei.com
  email: perfree@126.com
## swagger����
swagger:
  description: BootPress �ӿ��ĵ�
  enabled: true
  scan: com.perfree.controller

## �˿ں�
server:
  port: 8080

## ����Դ����
spring:
  datasource:
    url: jdbc:mysql://192.168.43.112:3306/perfreeblog?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: 215521..
    driver-class-name: com.mysql.cj.jdbc.Driver
    druid:
      initial-size: 5
      max-active: 20
      min-idle: 5
      max-wait: 60000
      filter:
        stat:
          db-type: mysql
          log-slow-sql: true
          slow-sql-millis: 2000
        config:
          enabled: true
        wall:
          enabled: true
          db-type: mysql
          config:
            delete-allow: true
            drop-table-allow: true
            multi-statement-allow: true
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.perfree.model
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

#pagehelper��ҳ���
pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql
